{
  "name": "@the-one-barber/engine",
  "version": "1.0.0",
  "private": true,
  "description": "The One Barber single engine — bookings, calendar, payments (PayFast), tiered loyalty, financials. One source of truth for all four dashboards.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
